import { BaseEntity } from '@cool-midway/core';
import { Column, Entity } from 'typeorm';

@Entity("{{snackCase namespace}}")
export class {{camelCase namespace}}Entity extends BaseEntity {
  {{#each entity}}
  @Column({{#if comment}}{ comment: '{{comment}}' }{{/if}})
  {{!-- badcase: number; --}}
  {{name}}{{#if tsType}}:{{tsType}}{{/if}}
  {{/each}}
}
